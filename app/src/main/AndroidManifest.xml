<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nisarg.ListIt">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/noteicon_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/noteicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication_my">
        <activity android:name=".taskList" />
        <activity android:name=".edit_product" />
        <activity android:name=".addItem" />
        <activity android:name=".Lists_shopping" />
        <activity android:name=".Answer_security_question" />
        <activity android:name=".setQuestions" />
        <activity android:name=".enterPattern" />
        <activity android:name=".About" />
        <activity android:name=".UpdateLockedNote" />
        <activity android:name=".Display_locked_notes" />
        <activity android:name=".PatternActivity_FirstTime" />
        <activity android:name=".UpdateNote" />
        <activity android:name=".Create_Note" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".NotificationPublisher" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>